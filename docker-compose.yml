version: '3.8'

services:
  crawler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dataset-crawler
    volumes:
      - ./output:/app/output
      - ./data:/app/data
    environment:
      - LOG_LEVEL=INFO
    working_dir: /app
    command: ./build/crawler
